# Nudge — Architecture Map

A file-level map of the project. Each entry is one responsibility + key relationships.
This is a map, not documentation — read the files for detail.

## Data flow (overview)

1. **User input** — the user brain-dumps in `HomeTabView` chat, or adds/edits tasks in `TasksTabView`, or imports a calendar/screenshot.
2. **Parsing** — `ClaudeService` (Haiku) turns free text into structured tasks/events; deterministic helpers (`DurationModel`, `EisenhowerScorer`, `StartByPlanner`) score/estimate without the LLM.
3. **SwiftData** — parsed items persist as `@Model`s (`NudgeTask`, `UserProfile`, `NudgeOutcome`, …) in the app-group container from `SharedModelContainer`.
4. **Enrichment** — `NudgeIntelligence` caches per-task AI signals (`TaskIntelligence`); `DayPlanEngine` (deterministic — the Tasks-tab button and the morning auto-run) or `DayPlanRefiner` (AI) place tasks onto today's timeline.
5. **Arbiter** — on data change / foreground, `NudgeArbiter.reevaluate` reads the data, applies gates (`BusyWindowResolver`, `NudgeConfig`), and decides which notifications should exist.
6. **Notifications** — `NudgeArbiter` registers every `UNNotificationRequest` (categories from `NudgeNotificationCategories`), the morning prompt included; `NudgeNotificationService` handles taps/actions/dismissals and writes back `NudgeOutcome`. Nudges the user never touches are resolved later by `NudgeOutcomeClassifier` on foreground.
7. **Sessions** — `SessionCoordinator` runs focus sessions and drives `LiveActivityManager` (Live Activity).
8. **Widgets** — the widget extension reads the shared store and renders task/companion widgets + Live Activities; App Intents write back completions.
9. **Feedback loop** — `NudgeOutcome` (behavioural `result`: tapped / dismissed / acted / engaged / ignored, plus an independent explicit `feedback`: markedUnhelpful from the 👎 notification action — the 👍 half was removed Jul 2026) and `CategoryDurationStats` feed future scoring/estimates. **The outcome half is RECORDING ONLY right now**: `NudgeConfig.fatigueGateEnabled` is `false`, so the per-task fatigue gate (its sole consumer since break-it-down's removal, Jul 2026) doesn't read the data it collects, and nothing at all reads `feedback` yet.
10. **Reevaluation is data-driven** — UI never schedules notifications directly; it mutates data and calls the arbiter.

---

## Cross-cutting invariants

Four things you cannot reconstruct by reading any single file. Read these first.

### 1. Ordered plans: stated order outranks Eisenhower score

`NudgeTask.sequenceIndex: Int?` is a 1-based position in the user's stated plan
("first X, then Y, then Z"); nil means the task is not in a plan. A plan is a
**numbered, reorderable list — not a placement**; it never sets a timeline slot.

**At every "what should I start?" decision point, the plan's next item (lowest
`sequenceIndex`) wins over score.** Since Jul 2026 the rule is BUILT INTO
`TaskSortComparator` (now in `Nudge/Models/NudgeTask.swift`, both targets) —
plan tasks are bucket 0, in stated order, ahead of every deadline bucket.
Sites that consume the comparator (`NudgeArbiter.buildIdleCandidates`,
`NudgeNotificationService.pickTopOpenTask`, `ContentView`'s widget-launched
session start, `NudgeWidget`'s row sort, the Tasks-tab session picker) get
plan-first for free instead of hand-bolting it. Two kinds of site still
implement the override themselves, deliberately:

- `NudgeArbiter.buildFloaterCheckInCandidates` (`floaterTargets`) — collapses
  to the plan's next item AFTER its placed-on-fire-day exclusion, an ordering
  the comparator can't express
- `DayPlanRefiner` + `DayPlanEngine` — rank non-plan tasks by
  Eisenhower `planScore`, not the comparator's deadline buckets; plan tasks
  lead in stated order and scored tasks follow

**One active plan at a time.** When a new ordered plan arrives from the brain
dump, `HomeTabView` clears `sequenceIndex` on every surviving task first. Those
tasks are not deleted — they drop back into the normal Unscheduled/Scheduled
sections, and the new plan owns 1,2,3…

`sequenceIndex` is independent of `plannedStartDate`: a task can be numbered in
Today's plan, placed on the timeline, both, or neither.

### 2. Schema registration: three lists, synced by hand

Adding or removing a `@Model` requires editing **three** separate lists. Nothing
enforces agreement — a mismatch is a runtime crash, not a build error.

| List | Location | Scope |
|---|---|---|
| `SharedModelContainer.schema` | `Nudge/Services/SharedModelContainer.swift` | the app's real container |
| `widgetSchema` | `NudgeWidget/NudgeWidget.swift` | the widget's own container — does **not** call `SharedModelContainer`, though that file is compiled into the widget target |
| `#Preview` container | `Nudge/ContentView.swift` | previews only — re-synced Jul 2026 after drifting four models behind (`CompletedTaskRecord`, `TimeBlock`, `CategoryDurationStats`, `EventDurationStats`) |

The widget re-declares its own schema, app-group ID, and `ModelContainer` rather
than reusing `SharedModelContainer`. The only guardrail is a comment in
`NudgeWidget.swift`: *"MUST stay in lockstep with `SharedModelContainer.schema`."*

### 3. Target boundaries are directory boundaries

The project uses Xcode's file-system-synchronized groups: dropping a `.swift`
file into `Nudge/` or `NudgeWidget/` auto-joins that target — no `.pbxproj` edit
**for the owning target only**.

The widget target additionally compiles all of `Nudge/Models/*.swift` plus
`Nudge/Services/SharedModelContainer.swift` and `Nudge/NudgeTheme.swift`
(Jul 2026 — so `WidgetColors` aliases the one palette instead of hand-copying
RGB triples), and **nothing else** from `Nudge/`. So widget code can reach the
models and the theme but not `NudgeConfig`, `DurationModel`,
`EisenhowerScorer`, or any other service.

That cross-target membership is NOT automatic: it's a hand-maintained
per-file `membershipExceptions` list in `project.pbxproj` ("Exceptions for
'Nudge' folder in 'NudgeWidgetExtension' target"). A **new** file in
`Nudge/Models/` joins the app target by itself but is invisible to the
widget until added to that list — the failure mode is a widget-only build
error naming the missing type.

### 4. NudgeOutcome lifecycle: `cancelAll` keeps past-due pending rows

`NudgeArbiter.cancelAll` runs on **every** reevaluate and used to delete
every `pending` outcome row. That destroyed the record of a delivered
notification minutes after it fired — which is why `.ignored` had no
writer anywhere and the fatigue gate could never trip. The rule now
splits on the fire time:

| `scheduledFor` | `cancelAll` | Why |
|---|---|---|
| **future** | deleted | The request it mirrors was still pending with the OS and was just removed. Deleting keeps the declarative rebuild honest: future pending rows always mirror the currently-scheduled set. |
| **past** | **kept** | The OS already delivered it. This row is the only evidence the nudge happened; `NudgeOutcomeClassifier` resolves it. |
| older than 14 days | deleted (any result, pending included) | Backstop so retained pending rows can't accumulate without a ceiling. |

A row leaves `pending` by one of two paths:

- **synchronously**, via `NudgeNotificationService` — the user tapped an
  action, tapped the body, or swiped it away (`.tapped*` / `.dismissed`).
- **later**, via `NudgeOutcomeClassifier` on foreground — for rows the
  user never touched. Grace period elapsed → look at the response window
  `[fireDate, fireDate + outcomeActionWindowMinutes]` → `.acted` (did the
  thing) / `.engaged` (opened the app, didn't) / `.ignored` (never opened).

`outcomeClassificationGraceMinutes` **must stay greater than**
`outcomeActionWindowMinutes`: that's what guarantees every window being
judged is already closed, so the foreground that runs the sweep can't be
counted as engagement with the row it's classifying.

**`.ignored` is not evidence of failure for every kind.** The classifier
infers it from "the app was never opened in the response window", which
only implies failure when success *requires* the app. It doesn't for
`.eventBlock`: the success case for "Class in 1 hour" is reading it and
going to class — no app open, and no completion either (events render via
`EventRowView`, which has no completion affordance, so the
`CompletedTaskRecord` branch of `performedAction` is unreachable for
them). Success and failure are behaviourally identical in the record.
`NudgeOutcomeKind.successIsObservableInApp` marks such kinds and the one
remaining fatigue consumer — the gate in `passesGates` — skips their rows.
This is a correctness exemption. (Until Jul 2026 there was a second
consumer, `buildBreakItDownCandidates`, plus a *policy* exemption for
`.breakItDown` as the gate's escape hatch; the break-it-down kind was
removed entirely, so past `perTaskMaxNudges` the arbiter now simply backs
off the task.)

**Two columns, two signals.** `NudgeOutcome.result` is behavioural (tap,
or inferred). `NudgeOutcome.feedback` is an opinion, written only by the
👎 notification action via `NudgeNotificationService.recordFeedback`,
which touches nothing else — in particular it leaves a `pending` row
pending so the classifier still sweeps it, and the feedback action is
non-`.foreground` so pressing it doesn't stamp `AppOpenLog`. A row can
therefore carry `.acted` + `markedUnhelpful` at once — the user did the
thing and still didn't want to be asked — which no behavioural reading can
express. **The channel is negative-only as of Jul 2026**: 👍 was removed
(nothing consumed it, no row ever carried it, and `DESIGN.md` puts positive
opinion in chat), so it can flag a rejected nudge but cannot confirm one
that quietly worked. Collection only — nothing reads `feedback` yet; the
DEBUG dump shows it in its own column plus an inferred×stated cross-tab.

**One `NudgeOutcomeKind` per feature — the kind IS the analytics identity.**
Two builders sharing a kind produce rows nobody can tell apart, which
silently costs you the ability to measure either one. That was the case
until Jul 2026: `buildFloaterCheckInCandidates` emitted `.getAhead`, the
same kind as `buildGetAheadCandidates`, though they do different jobs
(floater = undated low-stakes work when there's free time; get-ahead =
dated work approaching a deadline). `.floater` is now its own kind and its
own `category.floater`. Adding a kind is **additive** — existing rows keep
their raw values, so pre-split history stays attributed to `getAhead`
rather than being retroactively reinterpreted. The DEBUG dump's by-kind
tally is what makes the separation legible.

The same treatment split `.getAhead` itself in Aug 2026 (cycle
2026-08-01-03): `.prep` ("start early" — the old builder under a new
identity, discretionary in every dimension) and `.dueSoon` ("this is
landing" — ~2h before a deadline, once per task via the `dueSoonHistory`
delivered-marker map, budget-exempt like event blocks, same-hour deadlines
batched into ONE notification, and the second structural
`successIsObservableInApp == false` kind). `.getAhead` survives as a
legacy case so pre-split rows keep their attribution; kind-level baselines
end at that cut date. Body taps also stopped writing `.tappedStart` the
same cycle — they write `.tappedOpen` ("came to look"), with
`.tappedStart` reserved for the explicit Start action; pre-cut
`tappedStart` rows blend the two meanings.

A new kind must be added to: the enum, `successIsObservableInApp` (an
exhaustive switch — the compiler catches it), `TodayTimelineView.markerLabel`
(likewise), and — if it needs action buttons — `NudgeNotificationCategoryID`
plus the array passed to `setNotificationCategories`, which is **not**
compiler-checked. `fatigueBlindRawValues` and the DEBUG by-kind tally both
derive from `allCases` and need no edit. The remaining kind-sensitive site
is `NudgeNotificationService`'s tap routing, which string-compares against
`.morningPrompt` only: every other kind falls through to the Tasks tab,
which is what a new task-targeting kind wants.

Because past-due rows survive, one `notificationID` can in principle match
more than one row. The delegate resolves this by taking the newest
still-pending match. (The only ID without a day stamp was break-it-down's,
removed Jul 2026 — but the newest-pending rule stays as the defensive
reading.)

---

## App entry & root views

- `Nudge/NudgeApp.swift` — `@main` App: builds the SwiftData container, sets the notification delegate, registers background refresh, kicks off `StakesBackfill`. Hosts `ContentView`.
- `Nudge/ContentView.swift` — Root router (onboarding vs `MainTabView`); handles deep links, scene-phase, the debounced `NudgeArbiter.reevaluate`, and `recordForegroundAndClassifyOutcomes()` (`AppOpenLog` stamp → `NudgeOutcomeClassifier` sweep → DEBUG dump) on both cold launch and foreground.
- `Nudge/Views/Tabs/MainTabView.swift` — Tab shell owning `selectedTab`; reacts to `deepLinkTab` to switch tabs from notification taps. Consumes the one-shot `landOnGoalsAfterOnboardingKey` app-group flag (set by `OnboardingViewModel.completeOnboarding`) so the first post-onboarding screen is the Goals tab.
- `Nudge/NudgeTheme.swift` — Central colors + Lexend fonts; used by every view. **Compiled into the widget target too** (Jul 2026, via `membershipExceptions`): `WidgetColors` in `NudgeWidget.swift` is now a set of aliases over this palette — its hand-copied RGB triples had drifted (off-by-one channels, divergent alphas). Includes `goalAccent`, which was previously an inline literal in both targets. Also owns the **day-slot tints** (Aug 2026): `daySlotFills` / `daySlotAccents` (six index-matched hues, wrapped via `daySlotFill(_:)` / `daySlotAccent(_:)`) plus `eventSlotFill` / `eventSlotAccent`. Deliberately very light and **fully opaque** — no `.opacity()` at the call site — because they're a wayfinding channel, not a priority signal, and must never out-shout `overdue`/`coral`, the only colors allowed to mean urgency. Each has a **completed variant** (`daySlotFillCompleted(_:)` etc.) mixed 72% toward `daySlotCompletedGrey` via `completed(_:)` — computed, not a third hand-picked array, so changing a base hue carries into its finished state instead of leaving another list to hand-sync. The blend stops short of full grey on purpose: a checked-off row should recede but stay matchable to its block, and that identification is the whole point of the slot colors. See `DaySlotPalette` for who gets which slot.
- `Nudge/NudgeFeedback.swift` — Haptics (`NudgeHaptics`), animation constants (`NudgeAnimation`), AND the completion-effect visual system: `TaskCompletionEffect`, `CheckboxBounceEffect`, `AnimatedStrikethrough`, `ParticleBurstView`, exposed as `.taskCompletionEffect(isComplete:)` / `.checkboxBounce(isComplete:)`. Used by task rows and timeline blocks.
- `Nudge/Views/MainAppMockView.swift` — Dead shim: `typealias MainAppMockView = MainTabView`, kept for backward compatibility after the tab views moved to `Views/Tabs/`. No mock content.

## Tabs

- `Nudge/Views/Tabs/HomeTabView.swift` — Brain-dump chat; calls `ClaudeService.sendChat`, writes `NudgeTask`s, triggers the arbiter. `isPlanIntent()` keyword-matches "plan my day" / "restructure" / … and routes to `handlePlanIntent()` → `DayPlanRefiner.refine(force: true)`, Pro/trial gated. Owns **plan supersession** (clears `sequenceIndex` on surviving tasks when a new plan arrives) and three capture rules: timeless events stay events (*"Previously we demoted timeless events to tasks; that hid genuine plans."*); floater detection requires `!isEvent`; a bare due date resolves to **23:59, not midnight** — otherwise a task "due today" is instantly overdue.
- `Nudge/Views/Tabs/TasksTabView.swift` — The biggest file. Layout order (Aug 2026, post-cycle-08 design feedback): header (no description line) → `TasksMessageBox` (chat affordance: a filled `primary` circle OVERTOP of its bottom-right corner — badge-style, costs no box height; the -07 full-width button and -08 in-box chip each lasted one cycle) → Today label (+ conditional Clear plan) + timeline → **Plan my day + Start Session side by side, one compact 44pt row** (the stacked 52pt pair pushed the lists below the fold; the active-session banner keeps its own full-width row; same-treatment pair still a known open visual question) → **tab strip** (unselected chips carry a content-indicator stroke — `primary`, or overdue red for the Overdue chip — when their tab has tasks/events behind it, via `tabHasContent`, same membership rules as the tab bodies) → **one list at a time**. The stacked sections were replaced by four tabs (cycle 2026-08-01-05): **Unscheduled** (open tasks not on *today's* timeline — includes stale non-today placements so they can't vanish; prep tasks collapse per exam behind a "Show N more study days" control reusing the Events collapse idiom, keyed by `expandedPrepExams`), **Today** (numbered drag-reorderable plan list + placed-today rows, one tab; empty state is a lone Plan my day button). Today's rows are **day-slot tinted** and, for the placed ones, **ordered by `plannedStartDate` rather than `TaskSortComparator`** (Aug 2026) — these rows describe a day already drawn left to right in the timeline above them, and the comparator's deadline buckets put a 9am block below a 4pm one whenever the later block was due sooner, which reads as a different plan from the one on the strip. The tint reaches the row through `TaskRowView.slotTint`, which takes the card fill ahead of the `.overdue`/`.approaching` treatments; no urgency signal is lost because `stakesBarColor` still paints the leading bar, and the fill was only ever its second copy. `slotTint` is nil in every other tab, which leaves those rows untouched. **Completed rows sink to the bottom of both groups** and keep a greyed version of their slot tint (`daySlotFillCompleted`), so a finished task is still matchable to its block instead of vanishing — which is also why `scheduledTasks` is built off `actionableTasks` rather than `sortedTasks`: that one drops completed rows, so checking off a placement used to remove it from Today while its block stayed on the strip above, reading as the list and the timeline disagreeing about the day. Note `movePlanTasks` renumbers by DISPLAY order, so dragging an open task into the sunk-completed zone lands it at the end of the open rows rather than literally where it was dropped. The plan numeral takes the slot ACCENT but still shows `sequenceIndex` — that's the number drag-reordering rewrites, and the plan's stated order is a different claim from where a task landed on the clock, **Events** (two *lenses*, not buckets: "This week" ≤7 days incl. past/dateless, "Important" `stakes == .high` any date — a non-high event beyond 7 days shows in neither lens, it lives in the Calendar tab), **Overdue** (past due, open, `stakes != .low`; label+count badge in `NudgeTheme.overdue` red when non-empty, standard colors at zero). Default tab Today, not persisted across launches. The "Completed (N)" control + `WeeklyCompletedSheet` are gone — completed history lives in Stats; the weekly `purgeOldCompletedRecords` sweep stays here (keyed on tab appear). Also: `planMyDay()` — now a thin delegate to `DayPlanEngine` as a REPLAN (releases today's auto placements, re-places; deliberately NOT merged with `clearPlan()`, the undo — one is the re-roll, the other removes the plan); `recordPlanOutcome` writes the refusal messages (`PlanOutcomeContext`); `movePlanTasks` rewrites `sequenceIndex`; the AI Refine **button is gone** (cycle 2026-08-02-01 — `DayPlanRefiner` runs only from Home chat's plan intent now; its rationale still renders through the message box via `todaysRationale()` on appear); placement/editor/`IdleStartConfirmationSheet`/`SessionTaskPickerSheet`. The editor (`TaskEditorSheet`) gained a **Duration section** (Sep 2026) — chips 30m–4h writing `estimatedMinutes`, off-chip values surfaced in the section header, clear-by-retap; guarded by `durationUserPicked` (the stakes pattern: only a tapped chip writes on save, so open-and-save can't clobber an AI-captured length), and an event's hand-set duration also feeds `BusyWindowResolver.recordDuration` (see `EventDurationStats`). (`TaskSortComparator` moved to `Nudge/Models/NudgeTask.swift` Jul 2026.) `TaskRowView`'s subtitle shows a generated-series row the DAY it belongs to instead of a countdown (cycle 2026-08-03-02 — "Today's session" / weekday within a week / date beyond; a missed session keeps its day label in the overdue color rather than a growing "N hours ago" ticker), sources prep + commitment alike; ordinary tasks keep `CountdownState.remainingLine`.
- `Nudge/Views/Tabs/CalendarTabView.swift` — Calendar connect + screenshot import; calls `CalendarService` / `ScreenshotCalendarImporter`.
- `Nudge/Views/Tabs/StatsTabView.swift` — Reads `DailyStats` / `CompletedTaskRecord` / `EngagementState` for progress display.
- `Nudge/Views/Tabs/SettingsTabView.swift` — Edits `UserProfile` (wake/bedtime, toggles); changes reach the arbiter via `ContentView`'s notificationToken task. Carries the DEBUG-only sections: entitlement overrides and the `NudgeOutcomeClassifierHarness` runner.
- `Nudge/Views/Tabs/GoalsTabView.swift` — Personal goals list (active `NudgeGoal`s, newest last) with add/edit/remove via `GoalEditorSheet` (title + emoji chips; remove is a real `modelContext.delete` behind a confirmation). Row subtitle: "Worked on N ago" from `lastActivityAt`, or the deliberate zero case "Set N ago" from `createdAt` — never a false lapse. Fifth side slot in `AppTabBar`; onboarding lands here via `MainTabView`'s one-shot flag.
- `Nudge/Views/Tabs/AccountTabView.swift` — Account / plan (Pro, trial) status from `UserProfile`.

## Onboarding

- `Nudge/Views/Onboarding/OnboardingCoordinatorView.swift` — Drives the onboarding step sequence; owns `OnboardingViewModel`.
- `Nudge/Views/Onboarding/OnboardingViewModel.swift` — Holds onboarding draft state and writes it into `UserProfile` / habits on completion.
- `Nudge/Views/Onboarding/OnboardingChatShell.swift` — Chat-style shell wrapping each onboarding step's mascot dialogue.
- `Nudge/Views/Onboarding/OnboardingMessage.swift` — Model for a single onboarding chat message.
- `Nudge/Views/Onboarding/SplashView.swift` — Launch splash shown before onboarding/main.
- `Nudge/Views/Onboarding/WelcomeStepView.swift` — Name/intro step; feeds `OnboardingViewModel`.
- `Nudge/Views/Onboarding/WakeUpStepView.swift` — Wake-time step → `UserProfile.wakeTime` / `morningCheckInTime`.
- `Nudge/Views/Onboarding/BedtimeStepView.swift` — Bedtime step → `UserProfile.bedtime`.
- `Nudge/Views/Onboarding/RoutineStepView.swift` — Recurring routines → `NudgeHabit`s.
- `Nudge/Views/Onboarding/GoalsStepView.swift` — Personal goals → `NudgeGoal`s.
- `Nudge/Views/Onboarding/CoachingStyleStepView.swift` — Coaching tone → `UserProfile.coachingStyle` / `personalityMode`.
- `Nudge/Views/Onboarding/CalendarImportStepView.swift` — Optional calendar connect during onboarding; calls `CalendarService`.
- `Nudge/Views/Onboarding/OnboardingCompleteView.swift` — Final step: requests notification permission, marks onboarding complete.
- `Nudge/Views/Onboarding/Components/ChatBubbleView.swift` — Onboarding chat bubble.
- `Nudge/Views/Onboarding/Components/MascotAvatarView.swift` — Mascot avatar image.
- `Nudge/Views/Onboarding/Components/TypingIndicatorView.swift` — Animated "typing" dots.
- `Nudge/Views/Onboarding/Components/OnboardingProgressBar.swift` — Step progress bar.

## Components

- `Nudge/Views/Components/AppTabBar.swift` — Custom floating tab bar; morphs into a send button via `ChatComposerStore`.
- `Nudge/Views/Components/CountdownLabel.swift` — Task deadline display: the app-side renderer of `CountdownState` (which moved to `Nudge/Models/CountdownState.swift`, Jul 2026); used by task/event rows and the timeline.
- `Nudge/Views/Components/ScreenHeader.swift` — Reusable tab header (title + optional subtitle — Tasks passes none since Aug 2026; its message box speaks instead).
- `Nudge/Views/Components/TasksMessageBox.swift` — The read-only message surface at the top of the Tasks tab (Aug 2026) — **the app's only place to speak**; every other surface takes input or shows data. Deliberately display-only (no field, no send — Home owns capture) and deterministic (no `ClaudeService`; works offline). `TasksMessageComposer.compose` picks the first state that applies: tapped-nudge explanation → overdue (named, with how long) → high-stakes approaching within `messageBoxHighStakesHorizonDays` → the one-time prep-tombstone note ("still want study time for X?" — per parent, commitment copy when the tombstone's parent matches a `NudgeCommitment` row; consumed on first RENDER via `PrepTombstone.noteShownAt`, freshness-windowed so it doesn't vanish mid-read) → the exam-prep announcement ("I've added study time" — created-today only, freshness-windowed after first render; both Aug 2026) → the commitment announcement ("X is set up — an hour a day", cycle 2026-08-03-01, own storage slot, same lifecycle) → the AI Refine rationale (folded in from the old banner, Aug 2026 — below the actionable facts, since the rationale is stable all-day context whose result already shows on the timeline) → resting (never blank). The two prep states' copy lives in standalone statics (`prepNoteMessage` / `prepAnnouncementMessage`) because the view detects "did this state actually render" by equality — that render detection, not mere existence, is what fires the consumption callbacks. `compose` also carries the **AI seam**: an optional `aiMessage: TasksMessage?` returned ahead of every deterministic state — ARMED in cycle 2026-08-04-03 by the goal-lapse HOOK: when the user arrives from the bait (tap context kind `.goalLapse`, which now carries `goalID` via a fourth app-group key), `TasksTabView.fetchGoalLapseHookIfNeeded` asks `ClaudeService.generateGoalLapseHook` (live call — the app is running, unlike notification copy; reads goal, elapsed phrase, open titles, ignored-nudge count; per-(goal, day) cached in `GoalLapseHookCache`) and passes the result here; until/unless it lands, `nudgeExplanation`'s `.goalLapse` branch is the deterministic hook (elapsed-time fact + small offer; zero case says "you set this N ago", never a false lapse). The box takes `startsExpanded` (goal-lapse taps open with detail visible — the full message is the point of the tap). A **gentle invite** state (`goalInviteMessage`, slot 6.5 — just above resting) offers one small step toward the longest-neglected active goal with no open linked task; no notification. Both goal states carry the **one-tap accept** (item 4): an always-visible in-box button (`onAcceptGoalOffer`) that creates a `goalStepMinutes` (20) task titled with its size ("20 minutes on X"), priority medium, stakes deliberately unset, undated/unscheduled (lands in Unscheduled; floater + planner cover it), `goalID`-linked, `source: "goalOffer"` — no form. Accepting clears the lapse tap context; the invite recomputes to nil on its own once the goal has an open linked task. Form factor (Aug 2026): a row IN the list, not a banner — task-row footprint (padding 16, `radiusCard`, shared insets) but **outlined** (`NudgeTheme.border`, no fill) where task rows are filled; collapsed truncates the headline at two lines, expanding grows the row in place. `MessageBoxCharacterSlot` (56×56 since cycle 2026-08-03-03 — 2× the row checkbox, sized for a character illustration; the slot sets the box's ~88pt height floor, deliberately taller than a task row) reserves the mascot square — one `Image?` parameter, grey `textPlaceholder` square until an asset exists. `MessageBoxChatShell` (cycles 2026-08-03-04/-05/-06) is a **VISUAL PROTOTYPE, all stubs**: the -04 plain card (dimmed backdrop, header with portrait + "Nudge", chat bubbles, `radiusSheet`, single hairline border) — the -05 framed-dialogue styling was REVERTED in -06 after a device look; do not reintroduce it unprompted. What survived -05: **one surface, every way in** — the character, the row/chevron tap, and the chat circle (post-08 design feedback: a filled `primary` circle straddling the box's bottom-right corner like a badge — replaced -08's in-box chip; rendered only when a shell is wired, costs the box no height) all open the shell (`onOpenShell` supersedes the in-place detail expansion when set, nil keeps the legacy toggle), the dialogue seeds from the real composed `TasksMessage` so the detail stays reachable, and `MessageBoxExpression` (neutral/asking/pleased — deliberately no worried case, never-shame in art) is the slot's parameter, its `image` property the ONE mapping real art lands in (the shell's header portrait tracks conversation state). Fake two-option question, free-text field, canned "nothing is saved yet" replies; no persistence, no `ClaudeService`, Home chat untouched; collapsed appearance unchanged. `TasksTabView` hoists the shell as a whole-tab overlay (`messageShellSeed`, non-nil = open) so the backdrop can catch outside taps; the floating tab bar still renders above the dim layer (accepted, prototype). Judged-then-wired-or-deleted; do not build on it. The tapped-nudge context is `TappedNudgeContext`, read from the app-group keys `NudgeNotificationService` writes on a body tap — the idle "Not yet" durable-intent pattern generalized, but **freshness-window (`messageBoxTapContextMinutes`), not one-shot**: a modal sheet must fire exactly once, a passive display should survive re-appearing. Collapsed shows the headline (2 lines); tap expands to the detail. `NudgeTheme` only; leading accent bar, deliberately not a card.
- `Nudge/Views/Components/TodayTimelineView.swift` — Horizontal day timeline (events, placed tasks, notification markers, now-line); reads SwiftData, calls back into `TasksTabView` for open/place/**complete** (long-press a placed block completes it via `.taskCompletionEffect`; tap still opens — simultaneous gestures). Blocks are `Button`s and the empty-space tap gesture sits on the *container*, because a parent gesture is lower priority than the child buttons. Its `@Query`s are **bounded in a custom `init`** (tasks capped 300; outcomes predicated to `resultRaw == "pending"`, capped 60): *"This view is ALWAYS mounted in the Tasks tab and re-reads on every 60s clock tick, so unbounded `@Query` here fetches the full task/outcome history into the main context on a hot path."* `eventMinutes` calls the shared `NudgeTask.eventDurationMinutes(in:)` against its bounded `@Query` (Jul 2026 — it used to hand-mirror the resolver's resolution order), so the drawn block and the arbiter's busy window read one implementation. **Blocks are tinted by day slot** (Aug 2026): the `slots: [UUID: Int]` map passed in from `TasksTabView` (see `DaySlotPalette`) picks each task block's `NudgeTheme.daySlotFill`/`daySlotAccent`, and the same tint fills that task's row under Today, so a block and its row are matched by color rather than by re-reading titles. Events take one desaturated `eventSlotFill`/`eventSlotAccent` outside the rotation — they're fixed points in the day, not steps in a plan. A task missing from the map falls back to the event tint. The task block's title moved from `primary` to `textPrimary`, which was `primary` only because the old fill was one flat blue-grey.

## Services

- `Nudge/Services/SharedModelContainer.swift` — Builds the app-group SwiftData `ModelContainer` + shared `UserDefaults`. Compiled into both targets, but **only the app actually uses it** — the widget re-declares its own schema/container (see Cross-cutting invariant 2).
- `Nudge/Services/ClaudeService.swift` — All Anthropic (Haiku) calls: brain-dump parsing, day-plan refine, prep plans, screenshot parsing. Called by Home/Tasks, `DayPlanRefiner`, `NudgeIntelligence`, `ScreenshotCalendarImporter`. The brain-dump system prompt is a **behavioral contract**, not just parsing instructions: event-vs-task classification rules (a deadline is not an event; an exam is an event but studying for it is a task), 10 labeled worked examples, the ORDERED PLANS (`sequenceIndex`) rules, the single-follow-up rule for timeless events, and a generated 21-day weekday→ISO-date lookup table — *"The model is BAD at computing 'which date is next Thursday' on its own (it put 'Thursdays and Fridays' on the wrong dates), so we hand it an exact table and forbid it from calculating."* Both capture schemas (brain dump + screenshot) also emit the optional `stakes` consequence field (tolerant-decoded via `TaskStakes.parse`) and, for exam-category events only, `prepLeadDays` — the coarse 3/7/14 study-lead band judged from the exam title alone (Aug 2026; no web search, no finer precision; off-band values die at `ExamPrepSweep.validLeadBand`). The brain-dump prompt also detects COMMITMENT shapes (cycle 2026-08-03-01): `commitmentShape` (splitWork/rate/quantity) + `commitmentDailyCount` ride the same call — no second round trip — with exactly TWO permitted follow-up questions (splitWork with no total → ask rough hours; rate/quantity with no end date → ask when it ends), combined with the timeless-events question into one closing message, capture-first; answers come back through the ordinary `task_updates` fields (plus `commitmentStartDay`, cycle 2026-08-03-02 — the model only MAPS the start-today-or-tomorrow answer; the question itself is composed and appended app-side by `HomeTabView`, because deciding whether to ask needs room/growth arithmetic the model is never trusted with). `sendChat` takes `knownCommitmentSizes` (built by `HomeTabView` from `NudgeCommitment` rows) so a previously-sized kind of work is never re-asked — the match is delegated to the model inside the one call. The definition of stakes itself lives in one place — the `stakesRuleText` static, interpolated into the brain-dump prompt AND into `classifyStakes(titles:)`, the batched array-in/array-out backfill classifier (`StakesBackfill`'s only AI call; matches responses by **index**, not echoed title, and requires the returned index set to cover the request exactly — a truncated or renumbered response is well-formed JSON, so nothing else would catch it). Edit this prompt carefully; it defines app-wide capture behavior.
- `Nudge/Services/NudgeIntelligence.swift` — Read-only cached AI signals per task (`TaskIntelligence`); single-flight enrichment via `ClaudeService`. Read by `NudgeArbiter` / scoring.
- `Nudge/Services/NudgeArbiter.swift` — The decision engine: `reevaluate` reads data, applies gates, schedules/cancels notifications, writes `NudgeOutcome`. Called by `ContentView`, tabs, `SessionCoordinator`, `DayPlanRefiner`. Idle and floater check-in candidates target the **plan's next item** (lowest `sequenceIndex`) ahead of score — see Cross-cutting invariant 1. **Idle's pre-fire activity check reads only real activity** (Jul 2026): a focus session started or a task completed in the 3h window. The third check — "a calendar event falls in the window" — was removed; an event's existence is not evidence of activity, it suppressed the paralysis check-in on every school-day morning, and after the day rollover tomorrow's class suppressed tomorrow's nudge. `hadCalendarEvent` survives DEBUG-only as the before/after seam. Firing during/near class remains the busy-window gate's job. `buildFloaterCheckInCandidates` emits its own `.floater` kind + `category.floater` (Jul 2026 — it used to emit `.getAhead`, making the two features' outcome rows indistinguishable; see Cross-cutting invariant 4). Its targeting excludes tasks with a `plannedStartDate` on the **fire day** — an undated task the user (or `planMyDay`) has put on the timeline is not floating, and the check-in used to announce it "is still open" hours before its own slot. Scoped to the fire day rather than `plannedStartDate != nil` so a task placed on a *different* day than the one being evaluated stays a floater for it (`PlacementRollover` clears stale placements at day start, so a non-nil placement is always today-or-later). Its toggle was split the same way and it now reads `floaterCheckInNotificationsEnabled` (defaults `true`) rather than sharing `taskDueSoonNotificationsEnabled` with get-ahead. `buildEventBlockCandidates` reads `eventReminderNotificationsEnabled` (Jul 2026, defaults `true`) — event blocks were the last kind with no per-kind switch, so silencing a pre-class heads-up cost you the master `notificationsEnabled` and every other nudge with it. Like the morning prompt it is budget-exempt, so the toggle has to be checked in the builder: the shared gates in `passesGates` are keyed on `countsAgainstBudget` and never see it. Also owns the **morning prompt** (`buildMorningPromptCandidates`, replaced the fixed NotificationScheduler kickoff): budget-exempt like event blocks, so it does its own gating — per-kind toggle, day-fullness via `BusyWindowResolver.dayLoad` (skip when ≥ `morningPromptBusyDayThreshold` committed), and fire-moment busy check; tap lands in Home chat. **It names the day's biggest task** (Jul 2026 — it used to ask "What do you want to get done today?", an open question spending the app's best delivery slot on something it can't use). `morningPromptRanking` is split out of the builder the way `floaterTargets` is, so `debugMorningPromptImpact` prints the real selection: primary key is `NudgeTask.stakes` read **directly** (work order item 1 keeps stakes out of `EisenhowerScorer` — scoring is unchanged), ties break on the existing logistic urgency curve at the fire date, undated scores 0, and `nil` stakes ranks **above** `.low` because "never classified" is absence of evidence where `.low` is a statement that the item is minor. This adds a **content gate**: with nothing open there is no candidate, where the old copy fired anyway. `taskID` stays **nil** on purpose — free for the classifier (its `.morningPrompt` branch returns before the `taskID` guard) but not for the fatigue gate, which would start counting an ignored morning prompt against the user's highest-stakes task the day `fatigueGateEnabled` flips. Attribution rides on `NudgeCandidate.namedTaskID` instead, a field nothing else reads. **A task steps aside after `morningPromptMaxConsecutiveDays` mornings running** (Jul 2026) — stakes doesn't change day to day, so without it one task wins every morning until it's done and the channel gets tuned out. **Two fallbacks, both load-bearing, both meaning "repeat instead":** if nothing else is open (one task, already named twice) the rule stands down rather than letting the day's anchor go silent; and if the best replacement is more than **one `morningStakesRank` tier** below the incumbent it stands down too, because "Biggest thing on your list: Clean my desk" with a high-stakes task open is the app contradicting its own ranking in the slot the user reads first — a repeat beats a false claim. So high↔medium and nil↔low rotate; high↔nil and high↔low do not. `rankPool` ranks both pools (all-open, and not-recently-named) with one implementation so the comparison is checkable. History is `morningPromptHistory` in App Group defaults, keyed by fire-day stamp, written in `schedule()` alongside `eventReminderHistory` and pruned in `cancelAll`. The fatigue gate (`passesGates`) skips rows whose kind fails `NudgeOutcomeKind.successIsObservableInApp` — see Cross-cutting invariant 4. **Break-it-down is gone entirely** (Jul 2026): the kind, `buildBreakItDownCandidates`, `category.breakDown`, `tappedBreakDown`, and the misnamed `deadlinePrepNotificationsEnabled` toggle + Settings row all retired together; the profile field survives as a deprecated tombstone column only. **Every gate in `passesGates` reads the CANDIDATE'S FIRE DATE, never `now`** (Jul 2026 — the last two exceptions, the activity cooldown and the active-session bail, were the same bug fixed in consecutive cycles). A gate keyed on `now` drops candidates scheduled days out because of something happening this minute, which the next reevaluate then silently rebuilds. `firesDuringActiveSession` and `firesInsideActivityCooldown` are the two window checks; both are also keyed on `countsAgainstBudget`, so a factual pre-class heads-up is never suppressed by the user being mid-session — it is exactly what someone heads-down needs. **Wake-anchored fire times, and rollover.** Every discretionary builder places its nudge at an offset from wake: morning prompt at +`postWakeQuietMinutes` (30m), prep (né get-ahead, split Aug 2026 into `buildPrepCandidates` + the budget-exempt `buildDueSoonCandidates`) at +`prepAnchorHoursAfterWake` (2h), idle at +`idleThresholdHours` (3h), floater at +`floaterCheckInHoursAfterWake` (6h). Morning prompt / idle / floater roll over with one shared idiom — `if fireDate <= now`, add exactly one day — which suffices because their anchor day is always *today*. `prepFireDate` generalises it to a bounded walk forward, because prep's anchor day comes from the deadline; the walk stops at the due date and returns nil rather than nudging "get ahead" on something already overdue. The floater joined that idiom in Jul 2026; it previously gave up with `guard fireDate > Date()`, and since `cancelAll` runs first on every reevaluate, any reevaluate past the anchor deleted the morning's scheduled check-in and rebuilt nothing — which is why `.floater` never produced a single outcome row. Rolling is safe without a delivered-marker because the candidate ID embeds the **fire day's** stamp. **Wake+6h is a known bad anchor and is deliberately not retuned yet** (documented on `NudgeConfig.floaterCheckInHoursAfterWake`): with the default 08:00 wake it fires at 14:00, so any event starting ~12:30–15:30 suppresses it — the fire time falls in a busy window (possibly a *merged* one neither event covers), or the event's own heads-up an hour ahead evicts it on min-spacing, since `pickWinners` seeds event-block reminders into the winner set before any discretionary candidate. It fires on a free day, not on a class day. The offset can't be retuned against evidence until `.floater` rows exist; `debugFloaterImpact` prints the before/after and names whichever gate or spacing rule stopped it. **Quiet hours (`passesQuietHours`, Jul 2026 — was `insideAwakeWindow`).** Reads `UserProfile`'s own quiet-hours fields, falling back to the sleep-derived window when the user hasn't set their own, so default behavior is unchanged minute-for-minute (boundaries included). Modelled as a **wrapping interval in clock minutes-of-day**, not as two `Date`s pinned to the fire date's day: the old form assumed the awake window never crosses midnight, so a bedtime at or past 00:00 put `bed−60m` on the previous day, emptied the window, and failed **CLOSED** — every discretionary candidate suppressed all day, silently. `start > end` is now just the normal overnight case. The one unresolvable input (a zero-length window) fails **OPEN**, matching how `BusyWindowResolver.dayLoad` returns nil on the same degeneracy. Applies only to `countsAgainstBudget` candidates; event blocks and the morning prompt are exempt and never saw this gate. `debugQuietHoursImpact` prints the old-vs-new gate verdict per candidate and flags a degenerate old window explicitly. **The come-back nudge** (`buildComeBackCandidates`, kind `.comeBack`, cycle 2026-08-03-03): one candidate at `lastEngagement + comeBackAfterDays` (3), wake+2h anchored, scheduled on EVERY reevaluate — the declarative rebuild is the suppression: engagement (app open via `AppOpenLog`, a tapped `NudgeOutcome` row — `.dismissed` deliberately excluded, a `CompletedTaskRecord` — widget completions included) triggers a reevaluate that pushes it forward, so it only FIRES after three quiet days; the anchor is computed from engagement evidence, never `now`, so a BGTask reevaluate mid-absence can't push it out. Copy is forward-looking only ("“Chem quiz” is coming up Friday. Your list is ready when you are." — absolute weekday, baked days pre-delivery), names the earliest upcoming event/deadline within `comeBackLookaheadDays` (6), falls back to the open count, stands down entirely with nothing to say. `taskID` nil (morning-prompt reasoning), `namedTaskID` carries the named item for the copy-cache swap. Discretionary — budget, quiet hours, spacing all apply; event blocks are untouchable by construction (budget-exempt, seeded into `pickWinners` first). Actionless category `category.comeBack` (eventBlock reasoning: the ask IS tap-to-open); tap falls through to the Tasks tab like every non-morning kind. **The placement lifecycle pair** (`buildPlacementLeadCandidates` / `buildPlacementMissedCandidates`, kinds `.placementLead` / `.placementMissed`, cycle 2026-08-04-02) — before them, NO builder fired on a timeline placement passing unstarted; the only notification-path read of `plannedStartDate` was the floater *excluding* placed tasks, so placing a task reduced its coverage to zero. `.placementLead` fires `placementLeadMinutes` (5) before a slot, clustering slots within `placementClusterGapHours` (2h — its own constant, same value as events, so the two tune apart) into ONE heads-up via the same `clusterEvents` chaining; no ASAP fallback and no marker map, because everything past the slot belongs to the other kind. `.placementMissed` is the persistent one: a series anchored to the SLOT (grace 30m, then every 120m — 60 would self-evict under 90m min-spacing — capped at 4 attempts, clipped to the slot's day), all pre-scheduled so it delivers when the app never runs; a slot that passed with nothing delivered fires one ASAP catch-up, guarded by the `placementMissedHistory` delivered-marker map (the eventBlock/dueSoon pattern). Both discretionary in every dimension with urgency floored at `placementUrgencyFloor` (0.75) — the user's own plan slipping is the urgency signal; both `successIsObservableInApp == true`; two kinds rather than one so "does the heads-up help start on time" and "does the follow-up recover a missed slot" get separate baselines (the floater/get-ahead lesson). Completing, unscheduling, or `PlacementRollover` ends the series declaratively. `debugPlacementImpact` prints the pre-placement winner set beside the actual one and names anything displaced. **The goal-lapse bait** (`buildGoalLapseCandidates`, kind `.goalLapse`, cycle 2026-08-04-03) — the first notification-path reader of `NudgeGoal`: fires when an active goal has gone `goalLapseAfterDays` (30) without activity, from `lastActivityAt` or `createdAt` (the zero case). The notification is deliberately near-contentless BAIT ("Got a minute?" — template-only, never AI copy, doesn't name the goal, actionless `category.goalLapse`); the weight lives in the message box the tap opens. Come-back mechanics (fire date from the goal's own evidence, so recorded activity pushes it out on rebuild) plus two wallpaper guards: the per-GOAL delivered map `goalLapseHistory` (own `goalLapseHistoryRetentionDays` prune horizon — the shared 2-day cutoff would erase the monthly cap; the cap only BINDS once the recorded instant has passed, so rescheduling a pending bait can't push it out) enforcing `goalLapseMinDaysBetweenPerGoal`, and ONE candidate per run (earliest-due goal). Wake+`goalLapseAnchorHoursAfterWake` (8h — the +1.5h→+7.5h band is occupied). `taskID` nil; `NudgeCandidate.goalID` (new field, `namedTaskID` convention) rides into userInfo as `goalID` so the tap context can carry the goal. `debugGoalLapseImpact` prints the pre-lapse winner set beside the actual one.
- `Nudge/Services/NudgeConfig.swift` — All tunable constants (thresholds, budgets, priors) the arbiter/scorers consult.
- `Nudge/Services/NudgeCopyService.swift` — AI-written nudge copy, generated AHEAD and cached (cycle 2026-08-03-03). iOS fixes notification text at scheduling time, so copy can't be generated at delivery — `NudgeCopyGenerator` batches ONE `ClaudeService.generateNudgeCopy` call for the top `copyGenTasksPerKind` (4) tasks per kind (morningPrompt stakes-first mirroring the arbiter's ranking, prep by nearest deadline inside `copyCacheDays`+2, floater in comparator order, plus one kind-generic idle line and one `comeBack` entry naming the earliest upcoming event/deadline; **eventBlock and dueSoon are never generated** — their bodies embed exact clock times computed at scheduling). Cache is an App Group defaults JSON blob (`NudgeCopyStore`, no @Model → no schema lists), valid `copyCacheDays` (3) days. **The arbiter only READS it, synchronously** (`resolvedBody` in `schedule()` — no API call in the arbiter's path, DESIGN.md): body-only swap (titles stay deterministic; the tier's ⚠️/🔴/ALL-CAPS styling operates on titles), validated per read — entry age, named task still open, deadline unchanged (to the minute) and still ahead of the fire date; any miss → the deterministic template, so no key/no network/empty cache behaves exactly as before. Triggers: first foreground of a new calendar day (ContentView's scene-active Task, AFTER calendar refresh + auto-plan so generation sees fresh data), a calendar import with `importedCount > 0`, and `ExamPrepSweep.run` creating tasks; throttled by day marker + `copyGenMinMinutesBetween` (15m) + `copyGenMaxPerDay` (6, runaway backstop). DEBUG prints generations-today + full cache contents each pass, and the template-vs-generated pair at every swap. Tone rules (facts not verdicts, propose never promise, no implied failure, no relative day words — copy may fire days after writing) live in the generation prompt itself.
- `Nudge/Services/EisenhowerScorer.swift` — Pure urgency×importance scoring + quadrant routing; used by the arbiter and planners. `importance` takes an optional `stakes:` term (weights in `NudgeConfig.stakesImportanceBonus`) that **no production call site passes yet** — the six that exist all omit it, so the number is byte-identical to the pre-stakes one. `explicit` statedUrgency and a positive stakes bonus are capped as a pair by `stakesSignalCombinedCap`: the two signals co-occur on the same tasks, so summing them counts one belief twice and spends 0.45 of a 1.0 budget straight into the clamp. `.low` is a negative term and is deliberately outside the cap — separation is cheaper to buy at the bottom of the range than at the saturated top.
- `Nudge/Services/StartByPlanner.swift` — Computes a task's recommended start-by time and deep-work classification; used by arbiter + planners. **`startBy` answers WHICH DAY, not what time of day.** Both branches inherit the deadline's clock time (the deep branch subtracts whole days; the shallow branch subtracts effort×2 hours), and bare due dates normalize to 23:59 — so consuming `startBy` as a fire time puts the nudge at 21:59–23:59. `NudgeArbiter.prepFireDate` re-anchors it; don't reintroduce a direct `plan.startBy` fire time.
- `Nudge/Services/DurationModel.swift` — Deterministic effort estimate (category prior + learned `CategoryDurationStats`); feeds urgency scoring.
- `Nudge/Services/BusyWindowResolver.swift` — Computes event busy windows (+buffers, merges) from events; used by the arbiter's conflict gate and planners. Also exports `dayLoad(on:wake:bedtime:)` → `DayLoad` — the reusable "how full is this day?" number (awake window via `DayWindow.resolve`; events only, placements excluded). Consumed by the morning-prompt gate. Its nil now means only "sleep window shorter than its quiet buffers"; past-midnight bedtimes get a real window since cycle 2026-08-02-02 (they used to nil out, silently failing the day-fullness gate open).
- `Nudge/Services/DayWindow.swift` — **The one derivation of a day's planning window** (cycle 2026-08-02-02): wake + `postWakeQuietMinutes` → bed − `preBedtimeQuietMinutes`, **wake-anchored** — a bedtime at or before wake on the clock belongs to the *next* calendar date, so bed 00:30/wake 08:00 runs 08:30→23:30 *today*. Replaces three drifted inline copies (`planMyDay`, `DayPlanRefiner.refine`, `dayLoad`), every one of which had the same-calendar-day bug that made Plan my day a permanent silent no-op for past-midnight sleepers. Derive the window here or not at all.
- `Nudge/Services/DayPlanRefiner.swift` — AI layer over Plan-my-day: builds a day summary, calls `ClaudeService.refineDayPlan`, applies placements (only `plannedStartDate` / `plannedDurationMinutes` / `plannedIsAuto` — never schedules notifications). Plan tasks lead the candidate list in `sequenceIndex` order, non-plan tasks follow by score. Pro/trial gated, cached once per day unless `force: true`. Called by Tasks tab + Home chat.
- `Nudge/Services/ExamPrepSweep.swift` — Turns exam-category calendar events into daily study tasks (Aug 2026, `DESIGN.md` "Study tasks from exam events") AND expands brain-dumped commitments into daily tasks (cycle 2026-08-03-01 — the general form of the same sweep; an exam is a commitment whose deadline is an event). On launch and scene-active (called from `ContentView` beside `PlacementRollover`, BEFORE the arbiter reevaluate; also from `HomeTabView` after any capture turn that changed tasks, so an answered question expands immediately). **Exam half:** each open exam event within its `prepLeadDays` band (3/7/14 off `NudgeTask.prepLeadDays`, default `NudgeConfig.defaultPrepLeadDays`) gets one `source == "prep"` task per remaining day up to (not including) the exam day — title "Study for <exam>", category exam, stakes high via `setStakesFromAutomation`, `estimatedMinutes` from the exam category prior, `linkedEventId` back to the event. A user-made study task for the exam (deterministic `userStudyTaskMatch`) blocks creation entirely. **Commitment half (`commitmentPhase`):** a captured task with `commitmentShapeRaw` whose numbers are known (splitWork needs total effort + deadline; rate/quantity need an end date) converts into a `NudgeCommitment` row + one `source == "commitment"` task per day, and the capture task is DELETED — the dailies replace it. Every generated daily (both halves) is due at 23:59 of its OWN day via `sessionDueDate` (cycle 2026-08-03-02 — start-of-day dues made today's session read "20 hours ago" from birth: `sortDeadline` reads bare dates as end-of-day but the row subtitle fed the raw dueDate to `remainingLine`). The first day comes from the start-today-or-tomorrow flow (same cycle, item 3): `startDayDecision` (pure over `DayWindow` room + `splitWorkPlan` growth) yields tomorrowOnly (no room today → silent tomorrow start), todayForced (skipping today grows split-work sessions → start today and say so in chat), or choice — only then does `HomeTabView` append the question to the capture reply and hold expansion (`commitmentStartAskedAt`; expires at day rollover). The answer arrives as `task_updates.commitmentStartDay` → `commitmentStartDate` → `NudgeCommitment.startDate`, which anchors every generation window at `max(today, startDate)`. Split work generates ONCE (`splitWorkPlan`: total ÷ days, rounded UP to half-hour blocks, floor/ceiling in NudgeConfig, front-loaded from today through the day before the deadline); rate/quantity roll a `commitmentHorizonDays` (14) window topped up each run, end date inclusive. Quantity dailies carry `targetCount`, and `applyQuantityCarry` rolls missed units forward capped at `commitmentCarryCapDays` × count (accumulator on `NudgeCommitment.carryUnits`, stamped onto today's task as `carriedCount`; past dailies are consumed/deleted exactly once — the carry IS the reschedule, and consuming is what makes the walk idempotent). **Shared between the halves:** idempotence by (parent, day-stamp) via `missingStamps`, `PrepTombstone` (its `examEventId`/`examTitle` read as "parent id/title" for commitments; deletions recorded via `recordDeletionIfGenerated`, né `recordDeletionIfPrep`), and the announce rule (separate App Group slots — `currentAnnouncement` / `currentCommitmentAnnouncement` — so same-morning news doesn't overwrite). The decision cores (`decide`, `splitWorkPlan`) are pure statics. Generated dailies are ordinary tasks downstream EXCEPT: `buildDueSoonCandidates` excludes both generated sources (self-imposed due dates, not deadlines), and `DayPlanEngine` places them only on their own day.
- `Nudge/Services/DayPlanEngine.swift` — **The deterministic Plan-my-day engine** (cycle 2026-08-02-02, extracted from `TasksTabView.planMyDay()` so planning no longer needs the view): window via `DayWindow.resolve`, fills `BusyWindowResolver` gaps around events and existing placements, 1-hour pre-event "get ready" buffer, `planMaxPlacementsPerRun` (4) placements max with `planMaxPrepPlacementsPerRun` (2) of them prep, 15-min spacing, never overwrites a manual placement. **A generated daily (`source == "prep"` or `"commitment"`) is a candidate only on its own day** (cycles 2026-08-02-03 / 2026-08-03-01 — exam-category scoring let tomorrow's block win today's gaps, un-spreading the sweep's whole point; ordinary dated tasks stay eligible early deliberately; the generated-placement cap `planMaxPrepPlacementsPerRun` counts both sources). **Displacement (cycle 2026-08-03-01 item 4):** a generated session with no in-band gap may evict at most ONE auto placement per run, and only when the eviction is simulated to actually open a gap the session fits (in-band) AND the evictee is strictly lower stakes (rank high > medium > nil > low). Equal-stakes helpful candidates displace nothing — the `noRoom(contention:)` outcome names the session and the message box tells the user to decide. Displaced tasks return to Unscheduled (never silently moved to another day) and the auto-plan announcement names them; events and manual placements are never eviction candidates (only `plannedIsAuto` intervals are tracked at all). **Placement respects `TaskTimeWindow`** (same cycle): each candidate's `effectiveTimeWindow` clamps the gap search to its band's hours (`NudgeConfig.daytime*/businessHours*`; businessHours also requires a weekday) — no in-band gap ⇒ left unplaced, never placed out-of-band, and the trace distinguishes "only out-of-band gaps" from "day full". `planToday(clearAutoFirst:)` is the button path (replan); `autoPlanIfNewDay` is the **morning auto-run** — first foreground of a calendar day (App Group day marker `nudge.autoPlan.lastRunDay`, stamped even on refusal: one attempt per day), skipped entirely if today already has auto placements, announces a placement via `PlanOutcomeContext(.planned, isAuto: true)` and stays silent on refusals. Called from `ContentView`'s scene-active Task **after** the awaited calendar refresh (ordering: purge → rollover → prep sweep → arbiter → calendar refresh → auto-plan → arbiter again if placed).
- `Nudge/Services/DayPlanner.swift` — **Dead code.** Referenced by zero call sites; only two stale comments elsewhere still name it (`TaskActivityAttributes.swift`, `TaskLiveActivityView.swift`). The real deterministic planner is `DayPlanEngine`.
- `Nudge/Services/NotificationScheduler.swift` — **Retired as a scheduler** (Jul 2026): bedtime planning removed, morning kickoff reborn as the arbiter's morning prompt. What remains: `lastFocusSessionStartedAtKey` (shared UserDefaults key SessionCoordinator writes / arbiter reads) and `cancelRetiredDailyNotifications()` — one-time sweep of the old `nudge.daily.*` REPEATING requests, which outlive the code that scheduled them and which the arbiter's `nudge.arb.`-scoped cancel never touches. Do not add scheduling back here.
- `Nudge/Services/NudgeNotificationService.swift` — `UNUserNotificationCenterDelegate`: handles taps/actions, routes to tabs/sheets, writes `NudgeOutcome` (body taps and the idle "Not yet" reply write `.tappedOpen` since Aug 2026; `.tappedStart` is the explicit Start action only). `pickTopOpenTask` prefers the **plan's next item** over score. The idle-"Not yet" intent is made durable by writing `pendingIdleTaskIDKey`/`pendingIdleTaskDateKey` to the app group (consumed by `TasksTabView` on appear/active) because *"The transient `.nudgeIdleNotYetTapped` post is only caught if TasksTabView is already mounted and subscribed — which it is NOT on a COLD LAUNCH from the notification tap."* The same pattern now also records **which nudge a body tap arrived from** (`tappedNudgeKindKey`/`tappedNudgeTaskIDKey`/`tappedNudgeDateKey`, Aug 2026, written only for taps routed to Tasks) — consumed by `TasksMessageBox` on a freshness window rather than one-shot.
- `Nudge/Services/NudgeNotificationCategories.swift` — Defines `UNNotificationCategory` + action buttons used by every arbiter notification. Every category carries `.customDismissAction`, without which iOS never delivers `UNNotificationDismissActionIdentifier` and the delegate's long-standing `.dismissed` branch is dead code. Also owns the **feedback action** (👎), non-`.foreground` so rating a nudge can't stamp `AppOpenLog` and pollute the inferred result. iOS renders at most **four** actions (expanded interface only), so feedback is always listed last, on every category that has actions at all. **Three removals landed Jul 2026** (`docs/plan/archive/2026-07-29-01-plan.md`): 👍 everywhere (nothing read it), the Break-it-down action everywhere (its feature is paused pending removal), and **every** action on `.eventBlock` — the event heads-up is an FYI before a class, budget-exempt and fired whether or not the user wants a decision from them, so its buttons invited an interaction the notification has no use for. `.eventBlock` now carries `actions: []` and keeps only tap-to-open and `.customDismissAction`. That leaves `getAhead` and `floater` identical in action list (Start / Snooze / 👎) and differing only in identity, and `morningPrompt` on 👎 alone; the `breakDown` category left with its kind (Jul 2026). `category.placementLead` (actionless, the dueSoon batch reasoning) and `category.placementMissed` (Start / Snooze / 👎, the prep/floater set — it names one task) joined in cycle 2026-08-04-02. **The category array passed to `setNotificationCategories` is not compiler-checked** — a new category left out of it silently ships a notification with no action buttons.
- `Nudge/Services/NudgeOutcomeClassifier.swift` — The launch/scene-active sweep that resolves delivered-but-untouched `NudgeOutcome` rows into `.acted` / `.engaged` / `.ignored` (see Cross-cutting invariant 4, including which kinds' `.ignored` means nothing). Also owns the DEBUG 7-day outcome dump — `RESULT` and `FEEDBACK` as separate columns, plus a result tally, a **by-kind result breakdown** (iterates `allCases`, so a kind that produced nothing shows as an explicit `—` rather than going missing), a feedback tally, and an inferred×stated cross-tab. Recording only — `NudgeConfig.fatigueGateEnabled` decides whether anything consumes it. Driven from `ContentView.recordForegroundAndClassifyOutcomes()`. `classifyPending` is a thin composition of two private halves — `sweepableRows` (the pending + past-grace fetch) and `resolve` (the per-row acted/engaged/ignored decision) — split so `NudgeOutcomeClassifierHarness` can drive each independently via the `debugResolve` / `debugSweepableIDs` DEBUG seams.
- `Nudge/Services/NudgeOutcomeClassifierHarness.swift` — **DEBUG only.** Seeds backdated `NudgeOutcome` rows covering every classifier branch, runs the real `resolve` over them, prints expected-vs-actual, deletes what it created. Reachable from the Settings tab's "Debug — classifier harness" button. It calls `debugResolve` on its OWN rows rather than `classifyPending` on purpose: the classifier's session-start and `AppOpenLog` inputs are global single-valued shared state that the harness must overwrite, and a real sweep would judge the user's genuine pending rows against that fake state — permanently, since a row leaves `pending` exactly once. Both shared keys are snapshotted and restored in a `defer`, and orphans from a crashed run are swept on the next start. Fixture spacing is derived from `outcomeActionWindowMinutes` / `outcomeClassificationGraceMinutes`, so retuning either reshapes the fixtures instead of silently invalidating them.
- `Nudge/Services/AppOpenLog.swift` — Capped, self-pruning array of app-foreground timestamps in App Group `UserDefaults`. Exists solely so the classifier can answer "was the app open during *this* window". Deliberately NOT a `@Model` (would be a fourth hand-synced schema list + a fetch on the foreground hot path) and deliberately not `EngagementState`, whose `lastAppOpenDate` is a single overwritten value and whose `preferredHours` carries no dates. Written from both `ContentView.onAppear` and the scene-active handler; opens within 60s collapse. `debugReplaceAll` (DEBUG only) bypasses both the collapse and the retention prune so the harness can install exact backdated stamps.
- `Nudge/Services/CalendarService.swift` — Apple Calendar / Canvas iCal import into `NudgeTask` events; rolling-window sync + past-event purge. The 21-day window extends **one day per day** (cycle 2026-08-02-01 — was weekly, which let the horizon decay to 14 days, the prep sweep's max lead band, so far-edge exams entered their lead window un-imported); `refreshRollingWindow` is genuinely incremental, importing only the slice past the `calendarSyncedThrough` cursor. Stamps deterministic stakes at import (`inferStakes`: keyword scan via `isHighPriorityEvent`, then category ladder) — synchronous and offline on purpose; no Claude call. Also stamps the event's **real duration** into `estimatedMinutes` via `importedDurationMinutes` (end − start, capped at `NudgeConfig.maxImportedEventDurationMinutes`, nil for all-day / zero-length) — before this, every imported event landed with no duration and `BusyWindowResolver` read a three-hour lab as 60 minutes busy.
- `Nudge/Services/ScreenshotCalendarImporter.swift` — Vision OCR → `ClaudeService` → `NudgeTask` events from a schedule screenshot.
- `Nudge/Services/LegacyPriorityNormalizer.swift` — One-shot idempotent repair (Jul 2026): folds the retired `"urgent"` priority into `"high"` on `NudgeTask` and `CompletedTaskRecord` rows. The prompt no longer offers "urgent" and `HomeTabView.normalizedPriority` guards both capture write paths, so after one pass the value is extinct. Called from `ContentView`'s launch task.
- `Nudge/Services/PlacementRollover.swift` — Day-rollover sweep (Jul 2026): clears any `plannedStartDate` dated before today, **manual placements included** — a placement is a slot on one specific day, and rolling a manual one forward would be the app re-asserting a plan the user made for a different day. Before this, an unfinished task placed on a past day matched no list section (Unscheduled checks `plannedStartDate == nil`, Scheduled checks `isDateInToday`), drew nowhere on the timeline, and was skipped by both planners — it silently vanished at midnight. Called from `ContentView` on cold launch and scene-active, next to `purgePastEvents` and **before** the arbiter reevaluate. DEBUG builds print each rescued task.
- `Nudge/Services/StakesBackfill.swift` — One-shot repair of the stakes signal on EXISTING rows, kicked off fire-and-forget from `NudgeApp`'s delegate. Two populations, both excluding completed rows: `stakesRaw == nil` (never classified) and `source == "calendar"` (stamped by `CalendarService.inferStakes`, whose category ladder sends anything non-school to "low" — a flight or a visa deadline reads as low stakes). Never touches `stakesIsUserSet` rows; every write goes through `setStakesFromAutomation`. Dedupes titles via `EventDurationStats.normalize` before classifying, so ~100 rows collapse to ~15 titles = one chunked `ClaudeService.classifyStakes` call. **Atomic**: all titles are classified before anything is written, and any failure (no API key, offline, a 429 surviving its one backoff retry, mangled JSON) abandons the pass having written nothing — stakes is an enhancement, never a blocker. `Mode` is the stage switch: `.dryRun` prints a per-row table to the DEBUG console and writes nothing at all (including the completion marker, so it repeats freely); `.apply` writes and records `nudge.stakesBackfill.completedVersion` in the app group. Scope boundary: calendar events imported *after* a successful pass still get the deterministic `inferStakes` value.
- `Nudge/Services/SessionCoordinator.swift` — Runs a single focus session (state, timers, widget publish); drives `LiveActivityManager`, calls the arbiter.
- `Nudge/Services/LiveActivityManager.swift` — Starts/updates/ends the focus-session Live Activity (ActivityKit); driven by `SessionCoordinator`.
- `Nudge/Services/CountdownClock.swift` — Single 60s ticker publishing `now`; observed by `CountdownLabel`, `TodayTimelineView`, `TasksTabView`, and `ContentView`.
- `Nudge/Services/EngagementTracker.swift` — Records app opens, streaks, inactivity escalation into `EngagementState`; called on launch/foreground.
- `Nudge/Services/ChatComposerStore.swift` — Observable bridge between the Home composer and the floating tab-bar send button.
- `Nudge/Services/FocusSessionIntents.swift` — App Intents for focus-session actions (start/pause/end). **App-target copy.** `PauseFocusSessionIntent` / `EndFocusSessionIntent` are declared again in `NudgeWidget/FocusSessionIntents.swift`; the two files are separate per-target copies that do not collide. Check which target you mean before editing.
- `Nudge/Services/TaskScheduler.swift` — Legacy/cleared stub (old scheduling engine removed; `TimeBlock` model retained).
- `Nudge/Services/SmartNotificationEngine.swift` — Legacy/cleared stub (superseded by `NudgeArbiter`).
- `Nudge/Services/NotificationMessageGenerator.swift` — Legacy/cleared stub (message copy now built in the arbiter).
- `Nudge/Services/FocusSessionManager.swift` — Deprecated placeholder (replaced by `SessionCoordinator` + `LiveActivityManager`).

## Models (SwiftData `@Model` unless noted)

- `Nudge/Models/NudgeTask.swift` — Core task/event record (title, due, category, `isInformationalEvent`); the central entity most services read/write. Owns **the one event-duration resolution**, `eventDurationMinutes` (Jul 2026): explicit `estimatedMinutes` → learned `EventDurationStats` per-title row → `NudgeTask.fallbackEventDurationMinutes`. Two overloads — `(modelContext:)` fetches; `(in:)` resolves against an already-fetched stats array for the timeline's 60s-tick hot path. Consumers: `BusyWindowResolver`, `TodayTimelineView`, `DayPlanRefiner`, the widget chart. It lives on the model because the widget compiles Models only; the fallback constant's single source lives here too, with `NudgeConfig.defaultEventDurationMinutes` forwarding to it. Also owns `sortDeadline` / `isOverdue` and **`TaskSortComparator` with plan-first built in** (Jul 2026 — see Cross-cutting invariant 1; the widget previously carried private copies of all three). **Three** independent scheduling concepts live here (cycle 2026-09-03-01 added the third): **placement** — `plannedStartDate` / `plannedDurationMinutes` / `plannedIsAuto` (a slot on today's timeline; `plannedIsAuto` distinguishes "Plan my day" placements from manual ones, so `clearPlan()` can remove only the auto ones) — **plan order** — `sequenceIndex` (see Cross-cutting invariant 1) — and **intent day** — `intendedDate`, the day the user MEANS to do the work, never a deadline: day-granularity, survives its day passing (a slipped intent is floater-check-in material, where a stale placement is clutter and gets swept), never set on events. None implies another, and none touches `dueDate`/`specificTime`, which remain the deadline — now meaning ONLY owed work: capture writes them solely when the model marks `dueKind: "deadline"`, and `hasDeadline` is the one-word read-site question. The countdown, overdue treatment, dueSoon nudges, and urgency scoring all key off deadline fields, so an intention can no longer fabricate time pressure. Also carries the canonical stakes signal — `stakesRaw`/`stakes` + `stakesIsUserSet`; every non-user writer must go through `setStakesFromAutomation` (deliberately absent from the init), which refuses to overwrite a hand-set value. `prepLeadDays: Int?` (Aug 2026) is the coarse study-lead band (3/7/14) the capture/screenshot classifier reads off an exam event's title — band-validated at every write via `ExamPrepSweep.validLeadBand`, nil = didn't say (deterministic calendar imports never can), consumed only by `ExamPrepSweep`. Cycle 2026-08-03-01 added two additive field groups: `commitmentShapeRaw`/`commitmentDailyCount` (capture-detected commitment shape on a PARENT task awaiting expansion — non-nil marks it for `ExamPrepSweep.commitmentPhase`, which converts it to a `NudgeCommitment` row + dailies and deletes it) and the quantity-count trio `targetCount`/`completedCount`/`carriedCount` (+ `effectiveTargetCount` = base + capped carry — "three applications a day" is ONE task with a count; the row renders per-unit checkboxes under 4, a counter at 4+, and completes only at the effective target). Cycle 2026-08-03-02 added `commitmentStartDate`/`commitmentStartAskedAt` (the start-today-or-tomorrow answer and the outstanding-question marker; an unanswered ask expires at day rollover).
- `Nudge/Models/UserProfile.swift` — User settings/preferences (wake/bedtime, toggles, Pro/trial); read by arbiter, scheduler, tabs. **One per-kind toggle per notification feature** — `floaterCheckInNotificationsEnabled` was split off `taskDueSoonNotificationsEnabled` (Jul 2026) for the same reason `.floater` was split off `.getAhead`: one switch over two features means opting out of either costs you both. `eventReminderNotificationsEnabled` (Jul 2026, defaults `true`) closed the last gap in that rule — `buildEventBlockCandidates` had no guard at all, so the only way to stop event heads-ups was the master `notificationsEnabled`. `dueSoonReminderNotificationsEnabled` (Aug 2026, defaults `true`) gates the due-soon half of the get-ahead split; prep (the direct descendant) keeps `taskDueSoonNotificationsEnabled`, whose name now lies — intent continuity beats the label, same convention as the floater split. `comeBackNotificationsEnabled` (cycle 2026-08-03-03, defaults `true`) gates the come-back nudge. `placementLeadNotificationsEnabled` / `placementMissedNotificationsEnabled` (cycle 2026-08-04-02, default `true`) gate the placement lifecycle pair — two switches, not one, because the persistent follow-up is the likeliest opt-out and silencing it must not cost the pre-slot warnings. A new non-optional field needs a **property-level default** (there's no `VersionedSchema`, so lightweight migration is what opens existing stores; a defaultless attribute fails at launch, not at build). Adding a toggle also means adding it to `ContentView.notificationToken`, or flipping it never triggers a reevaluate. **Quiet hours are their own setting** (`quietHoursFollowSleepSchedule` + `quietHoursStartTime` / `quietHoursEndTime`, Jul 2026): they *default* to the sleep schedule but are no longer derived from it, because bedtime is when you go to bed and not necessarily when you want to stop being interrupted — an 11pm bedtime silenced a user who works until 12:30 during the exact hours they were working. The legacy `quietHoursStart` / `quietHoursEnd` `Int`s in the deprecated block are **not** these and remain read by nothing; they were not reused because `Int` hours can't express `bedtime − 60m` and every existing store already holds the init defaults (22 / 8), so consuming them would have changed the window for every current user rather than preserving it.
- `Nudge/Models/NudgeOutcome.swift` — Audit log of each emitted notification and the user's response. `result` is written by two different owners with two different lifetimes — see Cross-cutting invariant 4 for which, and for what `cancelAll` does and doesn't delete. Carries a **second, independent** `feedback` column (explicit 👎 only since Jul 2026) so behaviour and opinion can't overwrite each other. `tappedBreakDown` and the `.breakItDown` kind are gone (Jul 2026) — no row ever carried either raw value, and the tolerant getters cover strays. Also owns `NudgeOutcomeKind` — one case per notification feature, since the kind is the only thing that makes a row attributable (see Cross-cutting invariant 4) — and `successIsObservableInApp`, the correctness predicate every fatigue consumer must respect.
- `Nudge/Models/PrepTombstone.swift` — Durable record that the user DELETED a generated daily task — exam prep OR commitment daily (its `examEventId`/`examTitle` fields read as "parent id/title" for commitment rows; historical names kept, cycle 2026-08-03-01): one row per (parent, day) pair, written by `ExamPrepSweep.recordDeletionIfGenerated` from the Tasks tab's two real deletion sites (the DEBUG wipe-everything helper deliberately doesn't tombstone). Survives the task's removal — which is the point: `ExamPrepSweep` never recreates a tombstoned day. `noteShownAt` backs the message box's one-time "still want study/daily time?" note (per parent; commitment rows get commitment copy via `PrepNoteContext.isCommitment`, resolved by the Tasks tab against `NudgeCommitment` rows). Registered in all three schema lists AND the widget's `membershipExceptions` in project.pbxproj.
- `Nudge/Models/NudgeCommitment.swift` — The durable anchor for an expanded commitment (cycle 2026-08-03-01): `CommitmentShape` (splitWork / rate / quantity, tolerant lowercase `parse`) + one row per expansion holding the end date, the answered numbers (`totalMinutes` — THE remembered answer the capture prompt's KNOWN COMMITMENT SIZES context serves back so a second module of the same course never re-asks — `dailyMinutes`, `dailyCount`), inherited stakes/category/timeWindow, and the quantity carry accumulator `carryUnits` (capped at every write). Generated dailies link back via `linkedEventId` = this row's UUID string. Rows are deliberately never purged at end date: an expired row generates nothing but keeps its answer (the memory). Registered in all three schema lists AND the widget's `membershipExceptions`.
- `Nudge/Models/TaskIntelligence.swift` — Cached AI signals per task (statedUrgency, suggestedFirstStep); written by `NudgeIntelligence`.
- `Nudge/Models/DaySlotPalette.swift` — NOT a `@Model`: assigns each of today's tasks a **day slot** (Aug 2026). Slot 0 is the earliest block on the timeline, slot 1 the next, then unplaced ordered-plan tasks continue the rotation by `sequenceIndex`. Pure ordering — no colors (those are `NudgeTheme.daySlotFills` / `daySlotAccents` / `eventSlotFill` and their `…Completed` variants), no fetching, no mutation; the caller passes the tasks it already has. In `Models/` and in the widget's `membershipExceptions` list for the same reason as `CountdownState` and `TaskSortComparator`: **the widget's day chart must tint a task the same color the app does**, and a second copy is a copy that drifts. **One caller per target owns the assignment** — `TasksTabView` computes it off its unbounded `@Query` and passes the map down to `TodayTimelineView` (whose own fetch caps at 300); the widget provider runs its OWN narrow fetch (`!isInformationalEvent && (plannedStartDate != nil || sequenceIndex != nil)`, cap 200) rather than reusing its capped, unsorted `openActionable + recentDone`. A slot is an INDEX into an ordered day, so one task missing from the input shifts every slot after it and a task starts drawing in its neighbor's color — that is the single failure this file's placement and its callers' fetches exist to prevent. Ties break on `id` so two blocks at the same minute can't swap tints on a 60s clock tick; completed tasks keep their slot rather than renumbering the rows around them.
- `Nudge/Models/CountdownState.swift` — NOT a `@Model`: the pure countdown/date-line state machine (display rules 1–7, the 48h days-away boundary, the 11pm–7am night window, `ordinalSuffix`/`formatClockTime`/time-ago phrasing). Moved out of `CountdownLabel.swift` (Jul 2026) so the widget compiles it — `WidgetCountdownFormatter` used to hand-copy the logic and had drifted to a 72h boundary in `countdown()`, disagreeing with the app and with its own `remainingLine`. Rendered by `CountdownLabel` app-side; delegated to by `WidgetCountdownFormatter`. In the widget's `membershipExceptions` list.
- `Nudge/Models/StatedUrgency.swift` — Enum (none/explicit) for language-derived urgency.
- `Nudge/Models/TaskCategory.swift` — Enum of task categories + helpers used across scoring/config.
- `Nudge/Models/TaskTimeWindow.swift` — Coarse **appropriateness band** for tasks (cycle 2026-08-02-03): `anytime` (default) / `daytime` / `businessHours` (raw value lowercase so the tolerant `parse` survives the AI's camelCase). Assigned by the capture classification (same call as stakes/prepLeadDays; tasks only, events null), stored raw on `NudgeTask.timeWindowRaw` (additive, nil default). `infer(title:category:)` is the deterministic fallback (`inferStakes` pattern) — conservative: `businessHours` needs a contact word AND an institution word, because a wrong `businessHours` blocks placement where a wrong `anytime` is invisible. Resolution is READ-time via `NudgeTask.effectiveTimeWindow` (explicit ?? inferred), never stamped — stored values stay exclusively "what the classifier said". Consumed by `DayPlanEngine` placement. First what-a-task-*is* field; the errand-pairing prerequisite. Widget-visible via the hand-maintained membershipExceptions list (invariant 3).
- `Nudge/Models/TaskStakes.swift` — Closed high/medium/low **consequence** signal ("stakes") + tolerant `parse` (unknown → nil, no catch-all case — nil means "never classified"). Stored raw on `NudgeTask.stakesRaw`. Data-only for now: written by capture/screenshot (AI), calendar imports (deterministic `CalendarService.inferStakes`), and `StakesBackfill` (the AI pass that repairs both of the above on existing rows). Not consumed by scoring yet: `EisenhowerScorer.importance` accepts it but nothing production passes it, and `NudgeArbiter.debugStakesImpact` (DEBUG) is what the weights get validated against first.
- `Nudge/Models/CategoryDurationStats.swift` — Learned per-category effort mean; written on completion, read by `DurationModel`.
- `Nudge/Models/EventDurationStats.swift` — Learned per-title event duration; read by the timeline / busy-window logic. **Written only by `BusyWindowResolver.recordDuration`, whose sole caller (Sep 2026) is `TaskEditorSheet`'s save path** — a hand-set event duration is treated as ground truth for future same-title events. Before that wiring, `recordDuration` had zero callers and the learned tier of `eventDurationMinutes` could never fire; every event without an explicit `estimatedMinutes` rendered at the 60-minute fallback (the "7–10pm event draws one hour wide" bug — fixed from the other end too: the capture prompt now computes `estimatedMinutes` from stated spans/end times like "work 4–9", earlier end wins on a fuzzy "9 or 10").
- `Nudge/Models/TimeBlock.swift` — Scheduled time-block record (retained for the schedule rebuild).
- `Nudge/Models/NudgeGoal.swift` — User goal captured in onboarding/chat. `lastActivityAt` (nil = never worked on — the load-bearing zero case) is written only through `NudgeGoal.recordActivity(goalID:at:in:)`, the shared forward-only helper both targets call: task completion (Tasks checkbox, widget `CompleteTaskIntent`, chat-driven `task_updates`) and `SessionCoordinator.startSession` on a goal-linked task. Tasks link via `NudgeTask.goalID` — a soft UUID reference set conservatively by the AI at capture (`ActiveGoalContext` refs G1/G2… in the prompt, `goalRef` echoed back, resolved to UUID at the write site in `HomeTabView`); a removed goal leaves links dangling and readers treat that as unlinked.
- `Nudge/Models/NudgeHabit.swift` — Recurring routine/habit with reminder time.
- `Nudge/Models/CheckIn.swift` — Morning/evening check-in record.
- `Nudge/Models/DailySession.swift` — Per-day brain-dump chat transcript (source for AI history).
- `Nudge/Models/DailyStats.swift` — Per-day completion/activity counters for Stats.
- `Nudge/Models/CompletedTaskRecord.swift` — Completed-task history (weekly) shown in Stats (the Tasks-tab "Completed (N)" control was removed in cycle 2026-08-01-05; the Tasks tab still writes records and purges pre-week rows on appear).
- `Nudge/Models/EngagementState.swift` — Streaks, app-open counts, escalation level; written by `EngagementTracker`.
- `Nudge/Models/NotificationEvent.swift` — Record of a notification lifecycle event (analytics/dedupe support).
- `Nudge/Models/SentNotificationFlag.swift` — Marker preventing duplicate sends of the same notification.
- `Nudge/Models/TaskActivityAttributes.swift` — ActivityKit attributes for the DayPlanner/task Live Activity.
- `Nudge/Models/FocusSessionAttributes.swift` — ActivityKit attributes for the focus-session Live Activity.

## Widgets (NudgeWidget extension)

Path note: this directory is `NudgeWidget/` at the repo root — a sibling of
`Nudge/`, not a subdirectory of it.

- `NudgeWidget/NudgeWidgetBundle.swift` — `@main` widget bundle registering all widgets + Live Activities.
- `NudgeWidget/NudgeWidget.swift` — Task widget (medium/large): timeline provider + views + `WidgetColors` (non-private, shared with the companion widget). Row sort matches the app — plan tasks first by `sequenceIndex`, then score/deadline. Also renders a mini day chart (`WidgetTimeBlock`, rolling 5-hour window with "now" 15% from the left) — **day-slot tinted since Aug 2026**: `WidgetTimeBlock` carries a `slot: Int?` from the shared `DaySlotPalette` (nil for events), and `WidgetColors` forwards to `NudgeTheme.daySlotFill/Accent` + their `Completed` variants rather than redefining them, so a block cannot be one color here and another in the app. Block labels moved from `.white` to `textPrimary`/`textMuted` (white was legible only on the old 85%-opacity blue fill, not on pale tints), and completed blocks went from `.opacity(0.3)` to `0.75` because the greyed fill now carries the done signal — 0.3 on top of it washed the surviving hue out twice. Widget task **rows** carry the same tint: `TaskSnapshot.slot` feeds `WidgetTaskRow.rowBackgroundColor`, where the slot fill outranks the completed and active-session washes (both of those still show their own chip — "Done" capsule, bolt/timer — so only the redundant fill is given up; the goal-fallback row keeps its green and has no slot anyway). Because a slot exists only for a task placed today or holding a plan position, the rows that light up are exactly the ones the app shows under Today, even though the widget list itself is comparator-sorted. The row background gained a `cornerRadius: 8` clip so the tint reads as a block rather than a full-bleed band, list spacing went 0 → 2, and the hairline divider is now drawn **only between two untinted rows** — where a tint is present its edge already separates them. **Declares the widget's own `widgetSchema` / `widgetAppGroupID` / `widgetModelContainer`** — see Cross-cutting invariant 2.
- `NudgeWidget/NudgeCompanionWidget.swift` — Small mascot widget that deep-links into the app; uses `WidgetColors` and an edge-to-edge `containerBackground` (a manual `.background` sits inside the system content margins and leaves an inset ring).
- `NudgeWidget/NudgeWidgetControl.swift` — Control Center control (start session / open app).
- `NudgeWidget/NudgeWidgetLiveActivity.swift` — Live Activity configuration + lock-screen/Dynamic Island layout.
- `NudgeWidget/TaskLiveActivityView.swift` — Live Activity content view for a task/focus session.
- `NudgeWidget/CompleteTaskIntent.swift` — App Intent to complete a task from the widget (writes to the shared store).
- `NudgeWidget/FocusSessionIntents.swift` — App Intents for focus-session control from the widget/Live Activity. **Widget-target copy** — see the `Nudge/Services/` entry above.
