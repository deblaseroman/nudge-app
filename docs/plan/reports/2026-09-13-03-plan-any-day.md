# Report — 2026-09-13-03 — Plan any day + stale zone accepted

**Plan:** `archive/2026-09-13-03-plan.md`
**Status:** complete (device look pending)
**Commits:** `37bf90d` (implementation); plan, this report, ARCHITECTURE.md
in the report commit

---

## What changed

- `Nudge/Services/DayPlanEngine.swift` — `plan(day:)` is the primary;
  `planToday` delegates. Every today-ism found and keyed to the planning
  day: `DayWindow.resolve(on: day)`, replan release, busy placements,
  pre-event buffers, prep/commitment own-day check, band bounds (the
  DAY's daytime/business hours and weekend-ness — `bandBounds` previously
  read `Date()`, which would have used today's weekday for Friday), and
  candidates. `scanStart = max(dayStart, now)` needed no branch — for a
  future day it resolves to the window start. Future-day placements:
  `plannedIsAuto = false` (staleness rule) and excluded from the
  displacement ledger.
- `Nudge/Views/Tabs/TasksTabView.swift` — "Plan tomorrow" capsule at the
  top of the Tomorrow lens; `planFutureDay` runs the engine + arbiter.
- `Nudge/Views/Tabs/CalendarTabView.swift` — "Plan this day" on future
  selected days, with an inline outcome note (placed N / nothing to
  place / no room / window unresolvable); note clears on selection
  change.

## Verification

- Both schemes build clean.
- Work-order item 5: no builder or gate changed; new future-day
  placements flow into machinery that was already day-correct
  (placementLead groups by day; PlacementRollover's invariant is
  today-or-later). The planner's DEBUG dump prints the day being
  planned. Device pass pending: plan tomorrow → placements land around
  tomorrow's events; survive the next morning's auto-run; new tasks
  fill around them.

## Deviations from the plan

- None — the plan was written from the agreed chat rulings and the
  implementation followed it.

## Noticed but not done

- "Clear plan" clears only TODAY's auto placements; a future-day plan is
  undone per-task (editor / manage sheet), not in bulk. Acceptable for
  now; a "clear this day" affordance can ride a later cycle if wanted.
- Planning day D doesn't consider tasks intended for D that are already
  PLACED on D (correctly — they're busy intervals), but a task intended
  for D and placed on a DIFFERENT day by hand is left alone entirely;
  edge case, likely correct, noted.
- The Tomorrow lens button always shows; it could hide when tomorrow is
  already fully planned, but "re-run to fill new gaps" is a legitimate
  repeat use.

## Open questions

- None from this cycle. The time map's remaining parked items are the
  two hand-attached-date siblings (out-of-window tasks; floater-date
  meaning), both awaiting Roman's ruling.
