# NEXT — Move the chat button into the message box

**Status:** APPROVED
**Cycle ID:** 2026-08-03-08
**Source:** design feedback

> On `fix/notification-threading-and-memory`. Do not push, do not merge.

---

## What to do

The chat button is in the wrong place. Move it **inside the message box,
bottom-right corner** — so it sits with the thing it opens rather than
below the timeline.

Remove it from its current position in the button stack. The remaining
order is: message box (now containing the chat button), timeline, Plan my
day, Start Session.

Size it to fit inside the box without crowding the character or the text —
smaller than a full-width button. Say what treatment you chose.

Still opens the same shell as every other tap target.

## Constraints

- `NudgeTheme` only.
- The collapsed box's existing content — character, headline, detail —
  shouldn't shift or truncate to make room. If it has to, say so.
- Both schemes build.

## Evidence it worked

- The chat button sits in the bottom-right of the message box.
- It's gone from the stack below the timeline.
- The box's other content is unaffected.

## Out of scope

- Distinguishing Plan my day from Start Session visually.
- Any further shell work.
- Merging or pushing.
