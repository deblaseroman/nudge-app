# NEXT — Revert the dialogue frame, keep the tap targets

**Status:** APPROVED
**Cycle ID:** 2026-08-03-06
**Source:** looked at it on device

> On `fix/notification-threading-and-memory`. Do not push, do not merge.

---

## What to do

The framed dialogue styling from `260c9a6` doesn't work visually. Revert
the frame, keep everything else.

**Revert:** the 2pt/1pt double-stroke frame, the flat in-frame dialogue
lines, and anything else purely in service of the retro look. Go back to
how the shell looked in `2026-08-03-04` — the plain expanded state.

**Keep:**
- The expression enum and its single mapping point (`2026-08-03-05` item
  2). That's structure, not styling.
- **Every tap target opens the shell** — the character, the chevron, and
  anywhere in the collapsed box. That part works.
- The seeding behaviour, so the detail stays reachable now that the
  in-place expansion is gone.

Use `git` to identify exactly what the styling commit changed rather than
reconstructing by hand — it's one commit and one file.

## Constraints

- Both schemes build.
- Collapsed box unchanged.
- No new theme constants; drop the local frame weights entirely.
- Home chat untouched.

## Evidence it worked

- The expanded shell looks as it did in `2026-08-03-04`.
- Tapping anywhere in the collapsed box opens it.
- The expression enum still routes through one mapping.

## Out of scope

- Any further styling. I'll say when.
- Real capture wiring, persistence, character art.
- Merging or pushing.
