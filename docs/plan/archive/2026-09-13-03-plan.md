# Plan — 2026-09-13-03 — Plan any day + stale zone accepted (as approved)

**Status:** APPROVED — Roman, "agree on both", ruling on the time map's
two open items.
**Cycle ID:** 2026-09-13-03

## Ruling 1 — Planning beyond today

- Doors: "Plan tomorrow" on the Tomorrow lens; "Plan this day" on a
  future calendar day (inline outcome note).
- Staleness rule: future-day placements are marked MANUAL — that day's
  morning auto-run respects them (never releases) and refills remaining
  gaps with whatever arrived since. Respect + refill, never silent
  deletion.
- Engine: `DayPlanEngine.plan(day:)` generalizes `planToday` (which
  delegates). Day-keyed: window, busy set, pre-event buffers, band hours
  and weekend-ness, candidates. Day-integrity generalized: "belongs
  elsewhere" = still-ahead intent on a different day; dateless plans are
  today's only; slipped intents fair game; deadline work eligible early.
  Displacement stays today-only (future days hold no auto placements).
  Zero AI, zero tokens.

## Ruling 2 — The midnight stale zone

Accepted as designed: sweeps run before anything renders on first open,
the ~6 am BGTask narrows the window, and midnight-timer mitigation is
complexity spent on sleeping hours. Map entry closes as ACCEPTED.

## Evidence

- Both schemes build. Device: Tomorrow lens → Plan tomorrow fills
  tomorrow's gaps around its events; the placements survive the next
  morning's auto-run and new tasks fill around them; a future calendar
  day plans from the calendar with the note reporting counts.
- Work-order item 5: new placements feed existing builders
  (placementLead/Missed already group by day); no gate/builder changed.
  The planner candidate dump prints the day being planned.
