# Orientation for the planner

**Verified against `83d4cf0` at cycle `2026-09-03-01`.** The census sections
below (marked ⚙) are derived from a source enumeration at that commit and
regenerated by cycle under a standing `CLAUDE.md` rule: any cycle that adds,
removes, re-anchors, or disables a builder, a gate, or an AI call site updates
them and this stamp in the same commit. The unmarked prose sections carry
judgment and are hand-written. If the stamp is many cycles old, say so in your
plan and ask for the relevant root doc.

You can read `docs/plan/` and nothing else. This file exists so you can reason
about the system without the code — enough to write a plan that isn't guessing,
not enough to skip asking. **When a decision turns on a detail below, ask for
the relevant root doc to be relayed rather than inferring.**

Protocol: [`README.md`](README.md). Current item: [`NEXT.md`](NEXT.md) —
**pending only if no file in [`reports/`](reports/) shares its cycle ID**;
check before treating it as live work.

---

## What the app is

iOS app (SwiftUI + SwiftData) that turns brain-dumped tasks into scheduled
ADHD-friendly notifications. Two targets: the app and a widget extension
sharing one store via an App Group.

**The arbiter is the product** — the differentiator is deciding *when to
interrupt someone*, not the task list or planner. Weigh proposals against
whether they improve the interruption decision.

## How the arbiter works

`NudgeArbiter.reevaluate` is declarative and runs end-to-end every time. UI
never schedules notifications; it mutates data and calls the arbiter.

1. **Cancel** everything the arbiter owns (IDs prefixed `nudge.arb.`).
2. **Build candidates** — ten builders, below.
3. **Gate** them.
4. **Pick winners and schedule.**

Because step 1 always runs first, a builder that fails to rebuild something
*deletes* it. Several past bugs were this shape.

### ⚙ The ten builders (census-derived)

Every discretionary nudge fires at an offset from **wake**, not a fixed clock
time (the two placement kinds anchor to their timeline slot, and due-soon to
its deadline). Each has its own Settings toggle, default ON, plus a master
switch. "Exempt" = bypasses the daily budget AND the shared gates — those
builders self-gate.

| Builder | Kind | Fires | Budget |
|---|---|---|---|
| Event blocks | `eventBlock` | 60m before the first event of a cluster | exempt |
| Morning prompt | `morningPrompt` | wake + 30m; names the day's biggest task; skips packed days; taps land in Home chat | exempt |
| Idle / session starter | `idle` | wake + 3h, if nothing started | counts |
| Prep ("start early") | `prep` | planner-picked day, wake + 2h | counts |
| Due soon ("this is landing") | `dueSoon` | 2h before a deadline; same-hour deadlines batch; once per task | exempt |
| Floater check-in | `floater` | wake + 6h; open tasks with NO deadline and no today-or-future `intendedDate` (a slipped intent day is a target; cycle 2026-09-03-01), excludes anything placed that day | counts |
| Come-back | `comeBack` | 3 quiet days after last engagement, wake + 2h; any engagement pushes it forward | counts |
| Goal lapse (bait) | `goalLapse` | 30 days after a goal's last activity (or its creation — the zero case), wake + 8h; monthly at most per goal, one candidate per run; the real message is in the Tasks message box the tap opens | counts |
| Placement heads-up | `placementLead` | 5m before a timeline slot; close slots share one | counts |
| Placement follow-up | `placementMissed` | 30m after a slot passes unstarted, repeating every 2h, ≤ 4 attempts, ends at midnight | counts |

Retired kinds you may see in old data or reports: `getAhead` (split into
prep/dueSoon, Aug 2026), break-it-down (removed entirely, Jul 2026 — no kind,
no builder; do not resurrect).

### ⚙ The gates, in evaluation order (census-derived)

Upstream: a 60s debounce on time-triggered reevaluations, and the master
notifications toggle. Then per candidate — the first four apply only to
budget-counting candidates:

1. **Active session** — would the nudge *arrive* during a running session?
2. **Activity cooldown** — would it land inside the 90m window after a
   session start?
3. **Quiet hours** — the user's own do-not-interrupt window (defaults to the
   sleep schedule); a degenerate window fails open.
4. **Busy windows** — merged calendar events plus buffers.
5. **Per-task fatigue — DISABLED** by `NudgeConfig.fatigueGateEnabled =
   false`. When armed: suppress a task after 3 ignored nudges, skipping
   candidates with no task and kinds whose success leaves no in-app trace
   (event blocks, due-soon).

Every gate reads the **candidate's fire date, never "now"** — a hard-won
invariant; a gate keyed on now silently deletes future candidates.

Winner selection then applies the daily budget (10) and minimum spacing (90m),
seeding exempt candidates first — so a discretionary nudge near an event can
be evicted on spacing by that event's own heads-up.

### ⚙ Where AI actually is (census-derived)

**The load-bearing rule: no API call in the arbiter's path.** Notification
timing and selection are reproducible from local data. Do not propose an API
call in that path; non-deterministic timing is undebuggable (`DESIGN.md`).

But "no AI anywhere near the arbiter" is false, and plans that assume it
mis-predict behavior. Seven live call sites (all through `ClaudeService`):

1. **Brain-dump capture** (`sendChat`) — dump → tasks, commitments, goal
   links. Feeds the data every builder reads. Since cycle 2026-09-03-01 it
   distinguishes DUE from START (`dueKind`): only owed work gets
   `dueDate`/`specificTime`; an intention gets `intendedDate` (+ a manual
   placement when a time was stated), so deadline-keyed builders (dueSoon,
   prep, urgency scoring) read only real deadlines, and captured timed
   intents feed the placement builders.
2. **Screenshot calendar import** — schedule image → events.
3. **Per-task intelligence** (`NudgeIntelligence`) — cached 7 days. Its
   `statedUrgency` signal is read (cache-only, synchronously) inside the
   idle, prep, and floater builders and **feeds importance scoring, hence
   winner selection**. AI-derived data, deterministic read.
4. **Stakes classification** (`StakesBackfill`) — writes `task.stakes`; the
   morning prompt ranks on stakes directly, so this also **reaches an
   arbiter decision** (which task the morning names — not the scorer; see
   not-armed below).
5. **Notification copy** (`NudgeCopyService`) — bodies pre-generated and
   cached; the arbiter swaps text at scheduling. Copy only, never timing.
6. **AI day-plan refine** (`DayPlanRefiner`) — writes timeline placements,
   which the placement builders and floater exclusion then read.
7. **Goal-lapse hook** — the one AI-written message-box message, generated
   live on app open after the bait; deterministic fallback covers no-key/
   offline. Reads arbiter state, changes nothing.

Every site degrades to a deterministic path with no API key.

### ⚙ What is deliberately not armed (census-derived)

These are considered states, not oversights. Proposing to "fix" one without
saying why it's now authorized will get pushback.

- **Stakes is not wired into scoring.** The scorer accepts an optional stakes
  input; every production call site omits it — only the DEBUG harness passes
  it, so live scores are identical to pre-stakes. Reading `task.stakes`
  directly (morning ranking does) is a different thing and is allowed.
- **Arming it is blocked on floater baseline data.** The rollover bug was
  fixed Jul 2026, but rows still never accumulated — capture dated
  everything, so no undated task existed to target (`floater: SKIP` on every
  run, Sep 2026 console). Cycle 2026-09-03-01 is the population fix: capture
  now writes intentions to `intendedDate` instead of `dueDate`, and the
  floater targets no-deadline tasks whose intent day (if any) has passed.
  Whether rows accumulate is now a real question for the first time; check a
  by-kind dump before believing they have. Wake+6h is also a known-bad
  floater anchor (midday events suppress it), deliberately not retuned until
  the evidence exists.
- **The fatigue gate is off** (`fatigueGateEnabled = false`). Outcomes are
  recorded and observed before anything consumes them.
- **Explicit feedback is 👎 only** (👍 removed Jul 2026), and nothing reads
  it yet beyond a DEBUG dump.
- **Write-only fields:** `NudgeGoal.frequency`, and four legacy notification
  toggles on the profile (evening check-in, wind-down, habit reminder,
  monthly check-in) that gate nothing — plus the documented
  `deadlinePrepNotificationsEnabled` tombstone.
- **Dead endpoint:** `ClaudeService.generatePrepPlan` has no production
  caller (referenced only in a comment).
- **`MessageBoxChatShell` is a visual prototype** — all stubs; don't build
  on it.

**There is a standing evidence rule:** any change that alters what the arbiter
does gets a DEBUG before/after comparison on real data before it goes live.
Plans that change arbiter behavior should say what comparison would falsify
them. If a change genuinely can't alter behavior, say that instead — the
reasoning gets reported either way.

## Which root doc holds what

You cannot read these. Ask for the relevant one to be relayed.

- **`CLAUDE.md`** — the authority on current state. Holds the numbered work
  order the section above summarizes, the list of known-but-unfixed bugs, build
  and verification commands, and hard invariants (e.g. a new data model must be
  registered in three hand-synced lists; new profile fields need property-level
  defaults or existing installs crash at launch). **Ask for this whenever a
  plan touches sequencing, or when the summary above is load-bearing.**
- **`ARCHITECTURE.md`** — file-level map: one responsibility per file, plus the
  data-flow overview. Ask when a plan needs to name specific files or call
  sites.
- **`DESIGN.md`** — product intent: positioning (assistant, never an
  authority — the app must never promise an outcome), the AI-at-the-edges rule,
  what stakes means, the two feedback channels (behavior and explicit opinion,
  never a survey), and the no-shaming rule. **Ask before proposing anything
  user-facing** — copy, what the user is told, when they're interrupted.
- **`ROADMAP.md`** — the index of outstanding work: what's queued and under
  consideration, as distinct from `CLAUDE.md`, which holds the constraints in
  force right now. Ask for it when a plan needs to fit alongside work already
  intended, or before proposing something new that may already be on it.

Reports in `reports/` are the other half of your context: they carry what
actually happened, including deviations and things noticed but deliberately not
done. Read the recent ones before planning.
